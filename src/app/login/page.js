import LoginForm from "@/components/login/LoginForm";

export const metadata = {
  title: "Connexion — LEG-MEN",
  description: "Se connecter à la plateforme LEG-MEN",
};

export default function LoginPage() {
  return <LoginForm />;
}
